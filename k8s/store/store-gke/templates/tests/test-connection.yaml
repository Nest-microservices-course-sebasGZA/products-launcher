apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "store-gke.fullname" . }}-test-connection"
  labels:
    {{- include "store-gke.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "store-gke.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
