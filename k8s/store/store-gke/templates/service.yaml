apiVersion: v1
kind: Service
metadata:
  name: {{ include "store-gke.fullname" . }}
  labels:
    {{- include "store-gke.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "store-gke.selectorLabels" . | nindent 4 }}
